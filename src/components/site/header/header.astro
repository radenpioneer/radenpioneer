---
import image from '~/assets/profile.jpg'
import { getPicture } from '@astrojs/image'
import Header from './header'

const imgSrc = await getPicture({
  src: image,
  widths: [65],
  formats: ['avif', 'webp'],
})
---

<Header imgSrc={imgSrc} />
